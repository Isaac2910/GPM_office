services:
  - type: web
    name: gpm-django
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn GPM.wsgi:application --bind 0.0.0.0:$PORT"
    autoDeploy: true
